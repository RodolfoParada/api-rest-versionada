#### 1. se guardan las tasks en la carpeta Theory
#### 2. se guarda el archivo practica en la carpeta practica junto con la tarea.
#### 3. se crea el archivo api-rest-completa-versionada.js para desarrolla la tarea.
#### 4. se instala express y inicializa npm init -y
#### 5. se desarrolla la tarea en un solo archivo y se instala lo siguiente
  #### npm install express jsonwebtoken express-rate-limit morgan winston swagger-ui-express swagger-jsdoc body-parser
  #### post http://localhost:3000/auth/login
  #### post http://localhost:3000/webhooks
  #### get/post http://localhost:3000/api/v1/productos
  #### get/post http://localhost:3000/api/v1/productos
  #### get http://localhost:3000/docs